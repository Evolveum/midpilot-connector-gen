# Copyright (C) 2010-2026 Evolveum and contributors
#
# Licensed under the EUPL-1.2 or later.

# TODO
services:
  midpilot-connector-gen:
    build:
      context: .
      args:
        BASE_IMAGE: ${CONNECTOR_GEN_BASE_IMAGE:-midpilot-connector-gen-base:python3.13-playwright1.58.0}
    container_name: midpilot-connector-gen
    env_file:
      - .env
    environment:
      LOGGING__LEVEL: info
    ports:
      - "${HOST_PORT:-8090}:8090"
